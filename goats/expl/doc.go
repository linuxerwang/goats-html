/*
 * Package for goats-html expression language.
 *
 * Supported expressions:
 * - go-expr
 * - go-expr if (condition)
 * - go-expr if (condition) else go-expr
 * - go-expr if (condition) elif (condition) go-expr elif (condition) go-expr
 * - go-expr if (condition) elif (condition) elif (condition) go-expr else go-expr
 */
package expl
